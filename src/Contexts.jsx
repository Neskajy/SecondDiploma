import { createContext, useContext } from "react";

export const BurgerContext = createContext(null);
export const uriHistoryContext = createContext(null);
export const ModalDataContext = createContext(null);
export const DangerZoneContext = createContext(null);
export const PlanEventModalContext = createContext(null);
export const AddGroupModalContext = createContext(null);
export const AddColumnModalContext = createContext(null);
export const OpenGradeModalContext = createContext(null);
export const UniversalModalContext = createContext(null);